<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.multifuncional">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Multifuncional">
        <activity android:name=".NumerosPrimosActivity"></activity>
        <activity android:name=".ConversorHorasActivity" />
        <activity android:name=".ConversorMoeda_activity" />
        <activity android:name=".Menu_activity" />
        <activity android:name=".Login_activity" />
        <activity android:name=".SplashScreen_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>